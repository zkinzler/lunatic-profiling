# ===========================================
# Lunatic Profiling - Environment Variables
# ===========================================

# Database (Neon PostgreSQL)
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL="postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require"

# NextAuth.js Configuration
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email Provider (Resend)
# Get your API key from: https://resend.com
# Optional: If not set, emails will be logged to console
RESEND_API_KEY="your-resend-api-key"
# Set this to override email recipient during development
RESULTS_DEV_RECIPIENT=""
# Email sender address (requires verified domain in Resend)
EMAIL_FROM="Lunatic Profiling <no-reply@yourdomain.com>"

# OpenAI Configuration
# Optional: If not set, mock analysis will be used
OPENAI_API_KEY="your-openai-api-key"

# Sentry Error Monitoring
# Optional: If not set, errors will only be logged to console
SENTRY_DSN=""
SENTRY_AUTH_TOKEN=""

# Admin Dashboard
# Required for accessing /api/admin/* endpoints
ADMIN_DASHBOARD_TOKEN="your-secure-admin-token"
